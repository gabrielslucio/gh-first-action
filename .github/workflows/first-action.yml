name: First Workflow
on: workflow_dispatch #this is an event type that starts the workflow manually
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World!"
      - name: Print goodbey
        run: echo "Done - bye!"
